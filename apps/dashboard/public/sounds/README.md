# Sound assets

`new-request.mp3` — played by `useSoundAlert` when a new guest request arrives
on the staff dashboard. The committed version is an empty placeholder; replace
with a real ~400ms chime asset before shipping.
