import { RoomList } from "@/components/rooms/RoomList";

/**
 * Renders the room and device management screen.
 *
 * @returns The room list view.
 */
export default function RoomsPage() {
  return <RoomList />;
}
