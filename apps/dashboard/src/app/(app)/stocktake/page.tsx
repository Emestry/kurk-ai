import { SessionsTable } from "@/components/stocktake/SessionsTable";

/**
 * Renders the stocktake session list page.
 *
 * @returns The stocktake sessions table.
 */
export default function StocktakePage() {
  return <SessionsTable />;
}
